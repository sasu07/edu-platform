# Baza de date (PostgreSQL / Neon)
DATABASE_URL=postgresql://user:password@host/dbname

# Cheie secretă JWT — generează una random: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=schimba-asta-cu-ceva-random-lung

# Port API (opțional, default 8000)
# API_PORT=8000
