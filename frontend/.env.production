# Încărcat automat de Vite la `npm run build` (mod production).
# Adresa backend-ului de producție — frontend-ul cheamă API-ul aici.
# NU e secret (e vizibil în bundle-ul compilat). Se comite în git.
VITE_API_URL=https://api.e2xacademy.ro
